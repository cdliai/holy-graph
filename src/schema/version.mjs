// @cdli/holy-graph — FSL-1.1-Apache-2.0 — (c) 2026 CDLI
// Single source of truth for the data.json schema version.
// Imported by src/schema/v1.ts (for TS types) and src/extract/index.mjs
// (for runtime emit). Bumping this integer bumps the contract — renderer
// supports current + previous major.

export const SCHEMA_VERSION = 1;
